<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:orientation="vertical"
    android:layout_height="match_parent"
    android:id="@+id/drawer_layout_mode"
    tools:context=".ModeActivity">

<LinearLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Activating dark mode on our application  makes the screen
 easier on the eyes, saves energy, and improves visual comfort.
So it's popular among users."
        android:textStyle="bold"
        android:padding="10dp"
        android:textSize="20dp"
        android:layout_marginTop="20dp"/>
    <Button
        android:id="@+id/dark"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="10sp"
        android:text=" MODE "
        android:textSize="18dp"
        android:backgroundTint="@color/white"
        android:textColor="@color/black"
        android:textStyle="bold"
        android:layout_margin="10sp"
        android:drawableEnd="@drawable/sun"/>

</LinearLayout>





</androidx.drawerlayout.widget.DrawerLayout>